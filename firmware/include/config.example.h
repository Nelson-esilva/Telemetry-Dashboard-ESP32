#pragma once

// Copie este arquivo para config.h e preencha com suas credenciais.
// config.h não deve ser commitado se contiver senhas reais.

#define WIFI_SSID "SUA_REDE_WIFI"
#define WIFI_PASSWORD "SUA_SENHA_WIFI"

// IP local do PC onde a API FastAPI está rodando (não use localhost)
#define API_HOST "192.168.0.100"
#define API_PORT 8000

#define DEVICE_ID "linha_01"
#define TELEMETRY_INTERVAL_MS 5000
